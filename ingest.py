from fastapi import APIRouter, UploadFile, File, Depends, HTTPExceptionimport boto3, osfrom db import get_dbfrom sqlalchemy.orm import Sessionfrom models import Document, Chunkfrom pypdf import PdfReaderimport pandas as pdfrom docx import Document as DocxDocumentfrom auth import verify_tokenimport openairouter = APIRouter()s3 = boto3.client("s3",    aws_access_key_id=os.getenv("S3_ACCESS_KEY"),    aws_secret_access_key=os.getenv("S3_SECRET_KEY"),    endpoint_url=os.getenv("S3_ENDPOINT"))BUCKET = os.getenv("S3_BUCKET")client = openai.OpenAI(api_key=os.getenv("OPENAI_API_KEY"))def chunk_text(text, size=800):    words = text.split()    return [" ".join(words[i:i+size]) for i in range(0, len(words), size)]def embed(text):    r = client.embeddings.create(model="text-embedding-3-small", input=text)    return str(r.data[0].embedding)def extract_text(file):    ext = file.filename.split(".")[-1].lower()    if ext == "pdf":        reader = PdfReader(file.file)        return " ".join(p.extract_text() or "" for p in reader.pages)    elif ext in ["xls","xlsx"]:        return pd.read_excel(file.file).to_string()    elif ext == "csv":        return pd.read_csv(file.file).to_string()    elif ext in ["docx","doc"]:        doc = DocxDocument(file.file)        return " ".join(p.text for p in doc.paragraphs)    raise HTTPException(status_code=400, detail="Unsupported file type")@router.post("/upload")async def upload(collection_id: int, file: UploadFile = File(...),                 user_id: int = Depends(verify_token), db: Session = Depends(get_db)):    s3_key = f"{user_id}/{file.filename}"    s3.upload_fileobj(file.file, BUCKET, s3_key)    file.file.seek(0)    text = extract_text(file)    doc = Document(filename=file.filename, s3_key=s3_key,                   owner_id=user_id, collection_id=collection_id,                   file_type=file.filename.split(".")[-1].lower())    db.add(doc)    db.commit()    db.refresh(doc)    for chunk in chunk_text(text):        c = Chunk(document_id=doc.id, collection_id=collection_id,                  content=chunk, embedding=embed(chunk))        db.add(c)    db.commit()    return {"document_id": doc.id, "status": "indexed"}

