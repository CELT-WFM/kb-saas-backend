from fastapi import FastAPI, Depends, HTTPExceptionfrom fastapi.middleware.cors import CORSMiddlewarefrom sqlalchemy.orm import Sessionfrom pydantic import BaseModelfrom db import Base, engine, get_dbfrom models import User, Collection, Chunkfrom auth import verify_token, hash_password, verify_password, create_access_tokenfrom ingest import router as ingest_routerimport stripe, os, jsonfrom fastapi import Requestapp = FastAPI(title="KB SaaS V1")app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])Base.metadata.create_all(bind=engine)stripe.api_key = os.getenv("STRIPE_API_KEY")app.include_router(ingest_router, prefix="/documents", tags=["Documents"])class AuthBody(BaseModel):    email: str    password: strclass SearchBody(BaseModel):    query: str    collection_id: int    top_k: int = 5@app.get("/health")def health(): return {"status": "ok"}@app.post("/auth/register")def register(body: AuthBody, db: Session = Depends(get_db)):    if db.query(User).filter_by(email=body.email).first():        raise HTTPException(400, "Email already registered")    user = User(email=body.email, hashed_password=hash_password(body.password))    db.add(user); db.commit()    return {"message": "User registered successfully"}@app.post("/auth/login")def login(body: AuthBody, db: Session = Depends(get_db)):    user = db.query(User).filter_by(email=body.email).first()    if not user or not verify_password(body.password, user.hashed_password):        raise HTTPException(401, "Invalid credentials")    return {"access_token": create_access_token({"user_id": user.id})}@app.post("/collections")def create_collection(body: dict, user_id: int = Depends(verify_token), db: Session = Depends(get_db)):    c = Collection(name=body["name"], owner_id=user_id)    db.add(c); db.commit(); db.refresh(c)    return {"id": c.id, "name": c.name}@app.post("/search")def search(body: SearchBody, user_id: int = Depends(verify_token), db: Session = Depends(get_db)):    chunks = db.query(Chunk).filter_by(collection_id=body.collection_id).all()    results = [{"content": c.content, "source": c.source_url} for c in chunks[:body.top_k]]    return {"results": results}@app.post("/webhooks/stripe")async def stripe_webhook(request: Request, db: Session = Depends(get_db)):    payload = await request.body()    sig = request.headers.get("stripe-signature")    try:        event = stripe.Webhook.construct_event(payload, sig, os.getenv("STRIPE_WEBHOOK_SECRET"))    except Exception:        raise HTTPException(400, "Invalid signature")    if event["type"] == "customer.subscription.deleted":        cid = event["data"]["object"]["customer"]        user = db.query(User).filter_by(stripe_customer_id=cid).first()        if user: user.is_active = False; db.commit()    return {"received": True}

